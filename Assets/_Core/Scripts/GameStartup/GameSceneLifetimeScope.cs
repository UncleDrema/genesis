using VContainer;
using VContainer.Unity;

namespace Genesis.GameStartup
{
    internal class GameSceneLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}